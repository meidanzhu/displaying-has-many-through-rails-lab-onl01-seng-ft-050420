<h1>Doctor</h1>
Name: <%= @doctor.name %>
Department: <%= @doctor.department %>
Appointments:
<ul>
  <% @doctor.appointments.each do |app| %>
      <li>
          <%= app.display_time %> with <%= link_to app.patient.name, patient_path(app.patient) %>
      </li>
  <% end %>
</ul>
